<?xml version="1.0" encoding="UTF-8"?>
<!-- SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bnd.dailyband.mybatis.mapper.AdminMapper">

  <select id="getMemberList" resultType="member">
    select * from MBR_INFO where MBR_ID != 'admin';
  </select>

  <update id="memberTY">
    update MBR_INFO set MBR_TY = 'ROLE_MEMBER' where MBR_ID = #{id}
  </update>

  <update id="managerTY">
    update MBR_INFO set MBR_TY = 'ROLE_MANAGER' where MBR_ID = #{id}
  </update>

  <update id="adminTY">
    update MBR_INFO set MBR_TY = 'ROLE_ADMIN' where MBR_ID = #{id}
  </update>

  <update id="memberStatusAc">
    update MBR_INFO set MBR_STTUS = 0 where MBR_ID = #{id}
  </update>

  <update id="memberStatusIn">
    update MBR_INFO set MBR_STTUS = 1 where MBR_ID = #{id}
  </update>

  <select id="getListCount">
    select count(*) from MBR_INFO;
  </select>
</mapper>